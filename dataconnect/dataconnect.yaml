specVersion: "v1"
serviceId: "musicapplication"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "musicapplication-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
